on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/*-python-alpine-psycopg2.yaml'
      - 'python-alpine-psycopg2/**'
  workflow_dispatch:

jobs:
  docker_build:
    permissions:
      contents: read
      packages: write
    uses: petrochenkov-ak-lab/.github/.github/workflows/base-docker-image.yaml@main
    with:
      image_name: 'python-alpine-psycopg2'
      image_tag: '3.14'
      dockerfile: './python-alpine-psycopg2/Dockerfile'
